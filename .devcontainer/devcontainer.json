{
    "name": "Go",
    "image": "mcr.microsoft.com/devcontainers/go:2-1.25",
    "runArgs": [
        "--userns=keep-id"
    ],
    "securityOpt": [
        "no-new-privileges=false"
    ],
    "initializeCommand": "mkdir -p \"${HOME}/.agents/skills\"",
    "mounts": [
        "source=${localEnv:HOME}/.agents/skills,target=/home/vscode/.agents/skills,type=bind"
    ],
    "postCreateCommand": "bash -c 'mkdir -p ~/.agents/skills ~/.claude; [ -L ~/.claude/skills ] || rm -rf ~/.claude/skills; ln -sfn ~/.agents/skills ~/.claude/skills'",
    "customizations": {
        "vscode": {
            "settings": {
                "go.toolsManagement.checkForUpdates": "local",
                "go.useLanguageServer": true,
                "go.gopath": "/go",
                "go.goroot": "/usr/local/go"
            },
            "extensions": [
                "aaron-bond.better-comments",
                "bierner.markdown-checkbox",
                "bierner.markdown-emoji",
                "bierner.markdown-footnotes",
                "bierner.markdown-mermaid",
                "bierner.markdown-preview-github-styles",
                "DavidAnson.vscode-markdownlint",
                "EditorConfig.EditorConfig",
                "golang.go",
                "streetsidesoftware.code-spell-checker",
                "yzhang.markdown-all-in-one"
            ]
        }
    },
    "containerEnv": {
        "HOME": "/home/vscode"
    },
    "remoteUser": "vscode"
}
